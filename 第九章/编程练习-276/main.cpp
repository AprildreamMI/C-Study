#include <stdio.h>
#include "p276_fn.h"

int main(void) {
  double x = 7.33, y = 5.66, c = 3.66;
  /*printf("未转换值之前%lf, %lf\n", x, y);
  larget_of(&x, &y);
  printf("转换之后的值之前%lf, %lf\n", x, y);*/

/*  printf("未转换值之前%lf, %lf, &lf\n", x, y, c);
  transitionThreeVariate(&x, &y, &c);
  printf("转换之后的值之前%lf, %lf, %lf\n", x, y, c);*/

  printf("在字母表中的位置为：%d\n", findeAbcIndex('*'));


  return 0;
}
